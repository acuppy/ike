name: QuadrantTimer
options:
  bundleIdPrefix: com.adamcuppy
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

packages:
  KeyboardShortcuts:
    url: https://github.com/sindresorhus/KeyboardShortcuts
    from: "2.0.0"

settings:
  base:
    SWIFT_VERSION: "5.10"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    PRODUCT_NAME: QuadrantTimer
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    GENERATE_INFOPLIST_FILE: NO
    INFOPLIST_FILE: Sources/QuadrantTimer/Info.plist
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_ENTITLEMENTS: Sources/QuadrantTimer/QuadrantTimer.entitlements
    ENABLE_HARDENED_RUNTIME: YES
    DEVELOPMENT_TEAM: ""

targets:
  QuadrantTimer:
    type: application
    platform: macOS
    sources:
      - path: Sources/QuadrantTimer
        excludes:
          - "Info.plist"
          - "*.entitlements"
    info:
      path: Sources/QuadrantTimer/Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.adamcuppy.QuadrantTimer
        INFOPLIST_KEY_LSUIElement: YES
        INFOPLIST_KEY_CFBundleDisplayName: Quadrant Timer
        INFOPLIST_KEY_LSMinimumSystemVersion: "14.0"
    dependencies:
      - package: KeyboardShortcuts
